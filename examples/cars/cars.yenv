type Environment;
name CarsEnv;
tickRate 100;
width 1000;
height 200;
wrap true; // Cars wrap back to the start when they reach the end

agents Car;

init()
{
    for (i = 0; i < 50; i++)
    {
        create(Car, i * 20 + rnd() * 10, 100);
    }
    return true;
}